HelloWorld.class // bytecode

Garbage Collection

Deallocation:
    - programmer's repsonsibility (C/C++)
    - System responsibility (Java):
        - is done automatically (system-level thred)
        - Checks for and frees memory no longer needed

// I believe that Developers are able to free memory manually as // well in Java


Vocabulary {
    JVM, JRE, JDK
    Compilers vs interpreters
    Portability
}